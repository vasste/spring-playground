<?xml version="1.0" encoding="utf-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:util="http://www.springframework.org/schema/util"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	   					   http://www.springframework.org/schema/beans/spring-beans.xsd
	   					   http://www.springframework.org/schema/util
	   					   http://www.springframework.org/schema/util/spring-util.xsd
	   					   http://www.springframework.org/schema/tx
	   					   http://www.springframework.org/schema/tx/spring-tx.xsd
	   					   http://www.springframework.org/schema/context
	   					   http://www.springframework.org/schema/context/spring-context.xsd
	   					   http://www.springframework.org/schema/aop
	   					   http://www.springframework.org/schema/aop/spring-aop.xsd">

    <context:property-placeholder location="classpath:hello-world.properties"/>

    <bean id="jdbcPoolBuilder" class="com.devexperts.tos.jdbc.JdbcPoolBuilder">
        <property name="driverClass" value="${DB_DRIVER}"/>
        <property name="url" value="${DB_URL}"/>
        <property name="login" value="${DB_USER}"/>
        <property name="password" value="${DB_PASSWORD}"/>
        <property name="initialSize" value="${DB_POOL_SIZE}"/>
        <property name="maxSize" value="${DB_POOL_SIZE}"/>
        <property name="checkoutTimeout" value="2000"/>
        <property name="traceEnabled" value="false"/>
        <property name="idleConnectionCheckPeriod" value="60000"/>
        <property name="idleStatementsCheckPeriod" value="300000"/>
        <property name="statementsIdlePeriod" value="900000"/>
        <property name="jdbcProperties">
            <props>
                <prop key="oracle.net.READ_TIMEOUT">${DB_READ_TIMEOUT}</prop>
            </props>
        </property>
        <property name="rootMARSNodeName" value="jdbc.primary"/>
    </bean>

    <bean id="rawJdbcPool" factory-bean="jdbcPoolBuilder" factory-method="getPool"/>

    <bean id="dbPool" class="org.springframework.jdbc.datasource.TransactionAwareDataSourceProxy">
        <property name="targetDataSource" ref="rawJdbcPool"/>
    </bean>

    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dbPool"/>
        <property name="defaultTimeout" value="300"/>
    </bean>

    <bean class="com.devexperts.tos.utilities.DaoHelper"/>

    <bean class="com.devexperts.tos.transaction.spring.DatasourceSpringTransactionHelper">
        <property name="dataSource" ref="rawJdbcPool"/>
    </bean>

    <context:component-scan base-package="com.devexperts.tos"/>
    <!--<context:load-time-weaver/>-->
    <context:spring-configured/>
    <context:annotation-config/>
    <!--<tx:annotation-driven transaction-manager="txManager"/>-->
    <tx:annotation-driven mode="aspectj"/>

    <bean id="springApplicationContext" class="com.devexperts.tos.Server"/>
</beans>